server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config-repo

splunk:
  hec:
    url: ${SPLUNK_HEC_URL:http://localhost:8088}
    token: ${SPLUNK_HEC_TOKEN:}
