<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- SERVICE_NAME is what Splunk's props.conf reads back out of the JSON to
         tell RentHub services apart under the single "renthub:service:log"
         sourcetype - see splunk/etc/apps/renthub_app/default/props.conf. -->
    <property name="SERVICE_NAME" value="eureka-server"/>
    <property name="LOG_DIR" value="${LOG_DIR:-/var/log/renthub/eureka-server}"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [reqId=%X{requestId:-none}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Tailed by the splunk-uf container (inputs.conf monitors
         /var/log/renthub/*/app.log) and forwarded to the "renthub_logs" index. -->
    <appender name="JSON_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/app-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"service":"${SERVICE_NAME}"}</customFields>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="JSON_FILE"/>
    </root>
</configuration>
